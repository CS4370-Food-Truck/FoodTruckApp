<h1>Editing truck</h1>

<% if logged_in? %>
  <%= render "form", truck: @truck %>

  <br>

  <div>
    <%= link_to "Show this truck", @truck %> |
    <%= link_to "Back to trucks", trucks_path %>
  </div>
<% end %>